# Dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# Next.js
/.next/
/out/

# Production
/build

# Env files — NUNCA commitear .env.local
.env*.local
.env

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Working notes — nunca subir ideas, borradores ni material personal al repo
/ideas/

# Imágenes pesadas en working dirs (no versionar)
*.HEIC
*.heic
docs/IMG_*
docs/crops/
docs/maigrets/
docs/*.jpeg
docs/*.jpg
docs/*.pdf
docs/maigret.jpg
linda*
public/hero-libros-old.jpg

# Credenciales y salidas de los scripts SEO (GSC) — NUNCA commitear
*-service-account.json
scripts/seo/output/
scripts/seo/input/

# Supabase CLI local state (no es código, cambia solo)
supabase/.temp/
supabase/.branches/

# Playwright
/test-results/
/tests/playwright-report/
/playwright/.cache/
.env.production.tmp
.env*

# Llaves locales (service accounts, etc.)
.secrets/
social/

# Scripts de diagnóstico de un solo uso (_check_*, _audit_*, _find_*...). Sirven para
# mirar la BD en el momento y no son parte del producto. Los ~200 que ya están
# versionados quedan hasta que se purguen aparte; de acá en adelante no entran más.
scripts/_*.mjs

# Conversaciones y material con datos de usuarios reales — el repo es PÚBLICO
docs/chat_*.md

# Artefactos HTML generados por scripts (regenerables)
docs/books_huerfanos_*.html
