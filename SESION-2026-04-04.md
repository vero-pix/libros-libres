# Sesion 4 de abril 2026

## Resumen

Se trabajó en mejoras de UI/UX para la página principal de tuslibros.cl. Análisis completo del sitio realizado.

## Cambios realizados

### Sidebar de categorías (CategoriesSidebar.tsx)
- **Sticky sidebar**: queda fijo a la izquierda mientras se hace scroll (`sticky top-4`)
- **Scroll propio**: scroll independiente si hay muchas categorías
- **Mejor estilo visual**: items con padding, bordes redondeados, fondo destacado para categoría activa
- **Tipografía mejorada**: `font-display` consistente con el sitio
- Ancho reducido de `w-64` a `w-56` para más espacio al grid

---

## Auditoría completa del sitio

### Estructura de navegación
- **Navbar** se importa individualmente en cada página (19 de 22 páginas)
- **Sin Navbar (correcto)**: login, register (header mínimo), `/listings/new` (redirect)
- **Oportunidad**: mover Navbar a un layout compartido para evitar 19 imports duplicados

### Todas las rutas (22 páginas)
| Ruta | Navbar | Notas |
|------|--------|-------|
| `/` | Si | Home con sidebar categorías + grid |
| `/admin` | Si | Dashboard admin |
| `/checkout/[id]` | Si | Checkout (necesita integrar MP split) |
| `/como-funciona` | Si | Landing informativa |
| `/faq` | Si | Preguntas frecuentes |
| `/historia` | Si | Caso de éxito / historia |
| `/listings/[id]` | Si | Detalle de libro |
| `/listings/new` | No | Redirect a /publish |
| `/mapa` | Si | Vista mapa de libros |
| `/mis-arriendos` | Si | Gestión arriendos |
| `/mis-libros` | Si | Gestión libros propios |
| `/mis-pedidos` | Si | Gestión pedidos |
| `/orders/[id]` | Si | Estado de orden |
| `/perfil` | Si | Perfil usuario |
| `/privacidad` | Si | Política privacidad |
| `/publish` | Si | Publicar libro |
| `/search` | Si | Resultados búsqueda |
| `/sobre-nosotros` | Si | About |
| `/terminos` | Si | Términos y condiciones |
| `/vendedor/[id]` | Si | Tienda del vendedor |
| `/(auth)/login` | No | Header mínimo (correcto) |
| `/(auth)/register` | No | Header mínimo (correcto) |

---

## Plan de 2 días — próxima sesión (5-6 abril 2026)

### Contexto
- **500+ libros** listos para cargar en la primera etapa
- **150+ vendedores** históricos de tuslibros.cl para re-contactar
- MercadoPago Split Payment es la pieza crítica que faltaba

### Ilustración animada — mejora pendiente
- Agregar imagen de fondo con estantería real/local detrás de la animación SVG
- Buscar en las imágenes de las páginas de contenido (`content/`) una estantería que sirva
- La animación SVG se superpone sobre la foto con overlay semi-transparente

### Día 1 (5 abril): MercadoPago Split Payment + Navegación

**Mañana — MP Split Payment (prioridad máxima)**
1. Implementar OAuth de MercadoPago para vendedores
   - Flujo: vendedor conecta su cuenta MP desde `/perfil`
   - Guardar `mp_access_token` y `mp_user_id` en tabla `users` (Supabase)
2. Crear endpoint `/api/mp/connect` para callback OAuth
3. Modificar checkout para usar split payment:
   - Crear preferencia con `marketplace_fee` (comisión tuslibros)
   - Dinero va directo al vendedor, fee se retiene
4. Webhook `/api/mp/webhook` para confirmar pagos y actualizar estado de orden

**Tarde — Navegación y UX**
5. Mover Navbar a layout compartido (eliminar 19 imports duplicados)
6. Sidebar de categorías en mobile (drawer/bottom sheet)
7. Mejorar experiencia de búsqueda (autocompletado)

### Día 2 (6 abril): Arriendos + Pulido final

**Mañana — Sistema de arriendos**
1. Flujo completo de arriendo: seleccionar período (7/14/30 días), garantía, checkout
2. Notificaciones de devolución (email o in-app)
3. Estado de arriendo en `/mis-arriendos`

**Tarde — Preparación para lanzamiento**
4. Carga masiva de los 500+ libros (script o admin panel)
5. Testing end-to-end del flujo completo: publicar → buscar → comprar/arrendar → pagar → despacho
6. Ilustración animada del manifiesto (mejoras visuales)

---

## Archivos clave para retomar
- `app/page.tsx` — home con sidebar + grid
- `app/checkout/[id]/page.tsx` — checkout (integrar MP split aquí)
- `app/perfil/page.tsx` — perfil (agregar conexión MP OAuth)
- `components/ui/CategoriesSidebar.tsx` — sidebar categorías (modificado hoy)
- `components/ui/Navbar.tsx` — navbar (mover a layout)
- `components/home/HomeShell.tsx` — shell con hero y secciones
- `lib/supabase/` — cliente Supabase
- `MODELO-NEGOCIO.md` — flujos de pago y comisiones detallados
- `ROADMAP.md` — roadmap general del proyecto

## Modelo de comisiones a implementar
- **Plan Libre**: 8% venta, 10% arriendo
- **Plan Librero** ($9.990/mes): 5% venta, 7% arriendo
- **Plan Librería** ($29.990/mes): 3% venta, 5% arriendo
- Fee se cobra al comprador, no al vendedor ("Libre, sin comisiones")
